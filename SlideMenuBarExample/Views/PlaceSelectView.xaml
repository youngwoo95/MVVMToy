<Window x:Class="SlideMenuBarExample.Views.PlaceSelectView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:SlideMenuBarExample.Views"
        mc:Ignorable="d"
        Title="PlaceSelectView" Height="450" Width="500">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition/>
            <RowDefinition Height="20"/>
        </Grid.RowDefinitions>

        <!-- 사업장 목록을 표시할 영역: 스크롤이 가능하도록 ScrollViewer 사용 -->
        <ScrollViewer Grid.Row="0" VerticalScrollBarVisibility="Auto">
            <ItemsControl ItemsSource="{Binding PlaceList}">
                <ItemsControl.ItemTemplate>
                    <DataTemplate>
                        <!-- 라디오 버튼: Content는 사업장 이름, IsChecked는 선택 상태에 바인딩 -->
                        <RadioButton Content="{Binding Name}"
                                     IsChecked="{Binding isSelected, Mode=TwoWay}"
                                     GroupName="BusinessPlaces"
                                     Margin="5"/>
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
            </ItemsControl>
        </ScrollViewer>
        <Button Grid.Row="1" Content="선택" Command="{Binding SelectCommand}"/>
    </Grid>
</Window>
