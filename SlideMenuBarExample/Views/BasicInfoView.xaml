<UserControl x:Class="SlideMenuBarExample.Views.BasicInfoView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:SlideMenuBarExample.Views"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <!-- 리소스에 카드 스타일 정의 -->
    <UserControl.Resources>
        <Style x:Key="CardButtonStyle" TargetType="Button">
            <!-- 기본 설정 -->
            <Setter Property="Background" Value="#4c515c" />
            <!--<Setter Property="BorderBrush" Value="#DDD" />-->
            <Setter Property="BorderBrush" Value="Black" />
            <Setter Property="BorderThickness" Value="1" />
            <Setter Property="Margin" Value="10" />
            <Setter Property="Padding" Value="10" />
            <Setter Property="Width" Value="100"/>
            <Setter Property="Height" Value="100"/>
            <Setter Property="Cursor" Value="Hand"/>
            <!-- 기본 그림자 효과 -->
            <Setter Property="Effect">
                <Setter.Value>
                    <DropShadowEffect Color="Black" BlurRadius="8" ShadowDepth="2" Opacity="0.2"/>
                </Setter.Value>
            </Setter>
            <!-- 버튼 템플릿 정의 (Border에 Effect를 TemplateBinding) -->
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}"
                             BorderBrush="{TemplateBinding BorderBrush}"
                             BorderThickness="{TemplateBinding BorderThickness}"
                             CornerRadius="8"
                             Effect="{TemplateBinding Effect}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center" />
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
            <!-- 마우스 오버 효과 -->
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Effect">
                        <Setter.Value>
                            <!--<DropShadowEffect Color="#353840" BlurRadius="12" ShadowDepth="4" Opacity="0.3"/>-->
                            <DropShadowEffect Color="Wheat" BlurRadius="12" ShadowDepth="4" Opacity="0.3"/>
                        </Setter.Value>
                    </Setter>
                </Trigger>
            </Style.Triggers>
        </Style>
    </UserControl.Resources>

    <!-- WrapPanel에 ClipToBounds="False"를 설정하여 그림자(효과)가 잘리지 않도록 함 -->
    <WrapPanel VerticalAlignment="Center" HorizontalAlignment="Center" ClipToBounds="False">
        <Button Style="{StaticResource CardButtonStyle}">
            <StackPanel>
                <Image Source="pack://application:,,,/ico/home.png" Width="20" Margin="0 0 0 5"/>
                <TextBlock Text="건물 관리" 
                        Foreground="White" 
                        HorizontalAlignment="Center" 
                        VerticalAlignment="Center" 
                        TextAlignment="Center"/>
            </StackPanel>
        </Button>
        <Button Style="{StaticResource CardButtonStyle}">
            <StackPanel>
                <Image Source="pack://application:,,,/ico/home.png" Width="20" Margin="0 0 0 5"/>
                <TextBlock Text="위치 관리" 
                 Foreground="White" 
                 HorizontalAlignment="Center" 
                 VerticalAlignment="Center" 
                 TextAlignment="Center"/>
            </StackPanel>
        </Button>
        <Button Style="{StaticResource CardButtonStyle}">
            <StackPanel>
                <Image Source="pack://application:,,,/ico/home.png" Width="20" Margin="0 0 0 5"/>
                <TextBlock Text="단위 관리" 
         Foreground="White" 
         HorizontalAlignment="Center" 
         VerticalAlignment="Center" 
         TextAlignment="Center"/>
            </StackPanel>
        </Button>
    </WrapPanel>
</UserControl>
